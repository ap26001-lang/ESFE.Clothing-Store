@model List<ESFE._Clothing_Store.EN.Clientes>
@using Microsoft.AspNetCore.Http
@{
    ViewData["Title"] = "Clientes - Maison Élite";
    Layout = null;
    var usuario = Context.Session.GetString("Usuario") ?? "Administrador";
    var inicial = usuario.Length > 0 ? usuario.Substring(0, 1).ToUpper() : "A";
    var totalClientes = Model?.Count ?? 0;
    var clientesVip = Model?.Count(c => c.id_estado == 1) ?? 0;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Clientes - Maison Élite</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,600;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet" />

    <style>
        :root {
            --sidebar-bg: #1c140e;
            --sidebar-hover: #2a1f17;
            --gold-accent: #c4a47c;
            --gold-light: #e5d3b3;
            --bg-main: #f8f6f2;
            --card-bg: #ffffff;
            --text-dark: #2b2b2b;
            --text-muted: #7f7f7f;
            --positive-green: #40826d;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Plus Jakarta Sans', sans-serif;
        }

        body {
            background-color: var(--bg-main);
            color: var(--text-dark);
            display: flex;
            height: 100vh;
            overflow: hidden;
        }

        .sidebar {
            width: 260px;
            background-color: var(--sidebar-bg);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 24px 16px;
            color: #d1d1d1;
        }

        .brand-header {
            padding: 0 12px 24px 12px;
            border-bottom: 1px solid rgba(196, 164, 124, 0.15);
        }

        .brand-title {
            font-family: 'Playfair Display', serif;
            color: var(--gold-accent);
            font-size: 1.4rem;
            letter-spacing: 2px;
            font-weight: 600;
        }

        .brand-subtitle {
            font-size: 0.75rem;
            color: var(--gold-light);
            letter-spacing: 1.5px;
            text-transform: uppercase;
            opacity: 0.8;
        }

        .nav-menu {
            list-style: none;
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .nav-item a {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 16px;
            color: #b0a8a0;
            text-decoration: none;
            font-size: 0.9rem;
            border-radius: 8px;
            transition: all 0.2s ease;
        }

        .nav-item.active a, .nav-item a:hover {
            background-color: var(--sidebar-hover);
            color: #ffffff;
        }

        .nav-item.active a {
            border-left: 3px solid var(--gold-accent);
            color: var(--gold-accent);
        }

        .nav-item i {
            font-size: 1rem;
            width: 20px;
        }

        .sidebar-footer {
            border-top: 1px solid rgba(196, 164, 124, 0.15);
            padding-top: 16px;
        }

        .logout-btn {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 16px;
            color: #b0a8a0;
            text-decoration: none;
            font-size: 0.9rem;
            border-radius: 8px;
            transition: all 0.2s;
        }

        .logout-btn:hover {
            background-color: rgba(217, 83, 79, 0.15);
            color: #d9534f;
        }

        .main-content {
            flex: 1;
            overflow-y: auto;
            padding: 32px 40px;
            display: flex;
            flex-direction: column;
            gap: 24px;
        }

        .header-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            color: var(--text-dark);
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background-color: var(--gold-accent);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }

        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
        }

        .metric-card {
            background-color: var(--card-bg);
            padding: 24px;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.03);
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .metric-label {
            font-size: 0.75rem;
            font-weight: 700;
            color: var(--text-muted);
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .metric-value {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 600;
            color: var(--text-dark);
        }

        .metric-sub {
            font-size: 0.78rem;
            color: var(--positive-green);
            font-weight: 600;
        }

        .btn-gold {
            background-color: var(--sidebar-bg);
            color: var(--gold-accent);
            border: 1px solid var(--gold-accent);
            padding: 10px 18px;
            border-radius: 8px;
            font-size: 0.9rem;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all 0.2s;
        }

        .btn-gold:hover {
            background-color: var(--gold-accent);
            color: #ffffff;
        }

        .content-card {
            background-color: var(--card-bg);
            padding: 28px;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.03);
        }

        .filter-bar {
            display: flex;
            gap: 16px;
            margin-bottom: 20px;
        }

        .search-input {
            flex: 1;
            padding: 10px 14px;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            font-size: 0.9rem;
            outline: none;
        }

        .search-input:focus,
        .filter-select:focus,
        .form-control:focus {
            border-color: var(--gold-accent);
        }

        .filter-select,
        .form-control {
            padding: 10px 14px;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            font-size: 0.9rem;
            outline: none;
            background: #fff;
            color: var(--text-dark);
        }

        .custom-table {
            width: 100%;
            border-collapse: collapse;
            text-align: left;
        }

        .custom-table th {
            padding: 14px 16px;
            font-size: 0.75rem;
            font-weight: 700;
            color: var(--text-muted);
            letter-spacing: 1px;
            text-transform: uppercase;
            border-bottom: 2px solid #f0f0f0;
        }

        .custom-table td {
            padding: 16px;
            font-size: 0.9rem;
            border-bottom: 1px solid #f0f0f0;
            vertical-align: middle;
        }

        .client-info {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .client-avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background-color: #f2eee6;
            color: #725b4e;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.85rem;
        }

        .client-name {
            font-weight: 600;
            color: var(--text-dark);
        }

        .client-id {
            font-size: 0.75rem;
            color: var(--text-muted);
        }

        .badge-status,
        .badge-vip {
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
        }

        .badge-status {
            background-color: rgba(64, 130, 109, 0.12);
            color: #40826d;
        }

        .badge-vip {
            background-color: rgba(196, 164, 124, 0.2);
            color: #8c6a38;
        }

        .btn-action {
            background: none;
            border: none;
            color: var(--sidebar-bg);
            cursor: pointer;
            font-size: 1.05rem;
            padding: 6px 8px;
            border-radius: 6px;
            transition: all 0.2s;
        }

        .btn-action:hover {
            background-color: #f0ecf4;
            color: var(--gold-accent);
        }

        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.6);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .modal-container {
            background: #fff;
            width: 480px;
            border-radius: 12px;
            padding: 24px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #eee;
            padding-bottom: 12px;
            margin-bottom: 16px;
        }

        .modal-header h3 {
            font-family: 'Playfair Display', serif;
            color: var(--sidebar-bg);
        }

        .close-modal {
            background: none;
            border: none;
            font-size: 1.2rem;
            cursor: pointer;
            color: var(--text-muted);
        }

        .form-group {
            margin-bottom: 14px;
        }

        .form-group label {
            display: block;
            font-size: 0.82rem;
            font-weight: 600;
            margin-bottom: 6px;
            color: var(--text-dark);
        }

        .modal-footer {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
            margin-top: 20px;
        }

        .btn-cancel {
            background: #e5e5e5;
            border: none;
            padding: 9px 18px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            color: #444;
        }
    </style>
</head>
<body>
    <aside class="sidebar">
        <div>
            <div class="brand-header">
                <div class="brand-title">MAISON ÉLITE</div>
                <div class="brand-subtitle">Administrador</div>
            </div>
            <ul class="nav-menu">
                <li class="nav-item">
                    <a href="@Url.Action("AdminDashboard", "Home")"><i class="fa-solid fa-compass"></i> Dashboard</a>
                </li>
                <li class="nav-item">
                    <a href="@Url.Action("AdminCatalogo", "Home")"><i class="fa-solid fa-book-open"></i> Catálogo</a>
                </li>
                <li class="nav-item active">
                    <a href="@Url.Action("AdminClientes", "Home")"><i class="fa-solid fa-users"></i> Clientes</a>
                </li>
                <li class="nav-item">
                    <a href="@Url.Action("AdminPuntoDeVenta", "Home")"><i class="fa-solid fa-cash-register"></i> Punto de Venta</a>
                </li>
                <li class="nav-item">
                    <a href="@Url.Action("AdminHistorialVentas", "Home")"><i class="fa-solid fa-clock-rotate-left"></i> Historial de Ventas</a>
                </li>
                <li class="nav-item">
                    <a href="@Url.Action("AdminBitacora", "Home")"><i class="fa-solid fa-list-check"></i> Bitácora</a>
                </li>
                <li class="nav-item">
                    <a href="@Url.Action("AdminRespaldos", "Home")"><i class="fa-solid fa-shield-halved"></i> Respaldos</a>
                </li>
            </ul>
        </div>
        <div class="sidebar-footer">
            <a href="@Url.Action("Index", "Home")" class="logout-btn">
                <i class="fa-solid fa-right-from-bracket"></i> Cerrar sesión
            </a>
        </div>
    </aside>

    <main class="main-content">
        <header class="header-bar">
            <div>
                <h1 class="page-title">Gestión de Clientes</h1>
                <p style="font-size: 0.85rem; color: var(--text-muted); margin-top: 4px;">Directorio visual de clientes y seguimiento comercial.</p>
            </div>
            <div style="display: flex; align-items: center; gap: 16px;">
                <button id="btnAbrirModalCliente" class="btn-gold">
                    <i class="fa-solid fa-user-plus"></i> Registrar Cliente
                </button>
                <div class="user-avatar">@inicial</div>
            </div>
        </header>

        <div class="metrics-grid">
            <div class="metric-card">
                <div class="metric-label">Total Clientes</div>
                <div class="metric-value">@totalClientes</div>
                <div class="metric-sub"><i class="fa-solid fa-arrow-up"></i> +12% este mes</div>
            </div>
            <div class="metric-card">
                <div class="metric-label">Clientes VIP</div>
                <div class="metric-value">@clientesVip</div>
                <div class="metric-sub"><i class="fa-solid fa-crown"></i> 27% del total</div>
            </div>
            <div class="metric-card">
                <div class="metric-label">Ingresos Acumulados</div>
                <div class="metric-value">$0.00</div>
                <div class="metric-sub"><i class="fa-solid fa-arrow-up"></i> +8.4% vs mes anterior</div>
            </div>
        </div>

        <section class="content-card">
            <div class="filter-bar">
                <input type="text" id="txtBuscarCliente" class="search-input" placeholder="Buscar por nombre, DUI o correo electrónico..." onkeyup="filtrarClientes()" />
                <select id="filtroCategoria" class="filter-select" onchange="filtrarClientes()">
                    <option value="">Todas las categorías</option>
                    <option value="VIP">VIP</option>
                    <option value="Regular">Regular</option>
                    <option value="Nuevo">Nuevo</option>
                </select>
            </div>

            <table class="custom-table" id="tablaClientes">
                <thead>
                    <tr>
                        <th>Cliente</th>
                        <th>DUI / Documento</th>
                        <th>Teléfono</th>
                        <th>Correo</th>
                        <th>Compras</th>
                        <th>Categoría</th>
                        <th style="text-align: center;">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Count > 0)
                    {
                        foreach (var cliente in Model)
                        {
                            var iniciales = string.Join("", (cliente.Nombre ?? string.Empty)
                                .Split(' ', StringSplitOptions.RemoveEmptyEntries)
                                .Take(2)
                                .Select(p => p.Substring(0, 1).ToUpper()));
                            var categoria = cliente.id_estado == 1 ? "VIP" : cliente.id_estado == 2 ? "Regular" : "Nuevo";
                            var badgeClass = cliente.id_estado == 1 ? "badge-vip" : "badge-status";

                            <tr>
                                <td>
                                    <div class="client-info">
                                        <div class="client-avatar">@iniciales</div>
                                        <div>
                                            <div class="client-name">@cliente.Nombre</div>
                                            <div class="client-id">CLI-@cliente.id_cliente</div>
                                        </div>
                                    </div>
                                </td>
                                <td>@cliente.Dui</td>
                                <td>@cliente.Telefono</td>
                                <td>@cliente.Correo</td>
                                <td><strong>0</strong> pedidos</td>
                                <td><span class="@badgeClass">@categoria</span></td>
                                <td style="text-align: center;">
                                    <button class="btn-action btn-editar-cliente"
                                            title="Editar"
                                            data-id="@cliente.id_cliente"
                                            data-nombre="@cliente.Nombre"
                                            data-dui="@cliente.Dui"
                                            data-correo="@cliente.Correo"
                                            data-telefono="@cliente.Telefono"
                                            data-rol="@cliente.id_rol"
                                            data-permiso="@cliente.id_permiso"
                                            data-estado="@cliente.id_estado">
                                        <i class="fa-solid fa-pen-to-square"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" style="text-align:center; color:#7f7f7f; padding:24px;">No hay clientes registrados.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </section>
    </main>

    <div class="modal-overlay" id="modalCliente">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Registrar Nuevo Cliente</h3>
                <button class="close-modal" id="btnCerrarModal">&times;</button>
            </div>
            <form asp-controller="Home" asp-action="GuardarCliente" method="post">
                <div class="form-group">
                    <label>Nombre Completo</label>
                    <input type="text" name="Nombre" class="form-control" placeholder="Ej: Sofía Martínez" required />
                </div>
                <div class="form-group">
                    <label>DUI / Documento</label>
                    <input type="text" name="Dui" class="form-control" placeholder="Ej: 00000000-0" />
                </div>
                <div class="form-group">
                    <label>Correo Electrónico</label>
                    <input type="email" name="Correo" class="form-control" placeholder="Ej: correo@ejemplo.com" required />
                </div>
                <div class="form-group">
                    <label>Teléfono</label>
                    <input type="text" name="Telefono" class="form-control" placeholder="Ej: +503 7000-0000" required />
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn-cancel" id="btnCancelarModal">Cancelar</button>
                    <button type="submit" class="btn-gold">Guardar Cliente</button>
                </div>
            </form>
        </div>
    </div>

    <div class="modal-overlay" id="modalEditarCliente">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Editar Cliente</h3>
                <button class="close-modal" id="btnCerrarModalEditar">&times;</button>
            </div>
            <form asp-controller="Home" asp-action="EditarCliente" method="post">
                <input type="hidden" id="editIdCliente" name="id_cliente" />
                <input type="hidden" id="editRolCliente" name="id_rol" />
                <input type="hidden" id="editPermisoCliente" name="id_permiso" />
                <input type="hidden" id="editEstadoCliente" name="id_estado" />

                <div class="form-group">
                    <label>Nombre Completo</label>
                    <input type="text" id="editNombreCliente" name="Nombre" class="form-control" required />
                </div>
                <div class="form-group">
                    <label>DUI / Documento</label>
                    <input type="text" id="editDuiCliente" name="Dui" class="form-control" />
                </div>
                <div class="form-group">
                    <label>Correo Electrónico</label>
                    <input type="email" id="editCorreoCliente" name="Correo" class="form-control" required />
                </div>
                <div class="form-group">
                    <label>Teléfono</label>
                    <input type="text" id="editTelefonoCliente" name="Telefono" class="form-control" required />
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn-cancel" id="btnCancelarModalEditar">Cancelar</button>
                    <button type="submit" class="btn-gold">Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        const modalCliente = document.getElementById('modalCliente');
        const btnAbrir = document.getElementById('btnAbrirModalCliente');
        const btnCerrar = document.getElementById('btnCerrarModal');
        const btnCancelar = document.getElementById('btnCancelarModal');
        const modalEditarCliente = document.getElementById('modalEditarCliente');
        const btnCerrarModalEditar = document.getElementById('btnCerrarModalEditar');
        const btnCancelarModalEditar = document.getElementById('btnCancelarModalEditar');

        btnAbrir.addEventListener('click', () => {
            modalCliente.style.display = 'flex';
        });

        btnCerrar.addEventListener('click', () => {
            modalCliente.style.display = 'none';
        });

        btnCancelar.addEventListener('click', () => {
            modalCliente.style.display = 'none';
        });

        document.querySelectorAll('.btn-editar-cliente').forEach(btn => {
            btn.addEventListener('click', () => {
                document.getElementById('editIdCliente').value = btn.getAttribute('data-id');
                document.getElementById('editNombreCliente').value = btn.getAttribute('data-nombre');
                document.getElementById('editDuiCliente').value = btn.getAttribute('data-dui');
                document.getElementById('editCorreoCliente').value = btn.getAttribute('data-correo');
                document.getElementById('editTelefonoCliente').value = btn.getAttribute('data-telefono');
                document.getElementById('editRolCliente').value = btn.getAttribute('data-rol');
                document.getElementById('editPermisoCliente').value = btn.getAttribute('data-permiso');
                document.getElementById('editEstadoCliente').value = btn.getAttribute('data-estado');
                modalEditarCliente.style.display = 'flex';
            });
        });

        btnCerrarModalEditar.addEventListener('click', () => {
            modalEditarCliente.style.display = 'none';
        });

        btnCancelarModalEditar.addEventListener('click', () => {
            modalEditarCliente.style.display = 'none';
        });

        function filtrarClientes() {
            const input = document.getElementById('txtBuscarCliente').value.toLowerCase();
            const filtroCat = document.getElementById('filtroCategoria').value.toLowerCase();
            const filas = document.querySelectorAll('#tablaClientes tbody tr');

            filas.forEach(fila => {
                const texto = fila.innerText.toLowerCase();
                const coincideTexto = texto.includes(input);
                const coincideCat = filtroCat === '' || texto.includes(filtroCat);
                fila.style.display = coincideTexto && coincideCat ? '' : 'none';
            });
        }
    </script>
</body>
</html>